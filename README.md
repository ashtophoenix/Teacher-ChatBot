# Teacher ChatBot
In this workshop, I continued learning about strings by building a Teacher Chatbot.
I used template literals and string interpolation to include variables and expressions in strings. I located the position of specific substrings within a larger string using the indexOf() method.

